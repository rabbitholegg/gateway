export const ERROR = {
  INVALID_ADDRESS: 'invalid or missing address',
  INVALID_SIGNER: 'invalid or missing wallet client',
  AUTH_TOKEN_FAILED_TO_FETCH: 'could not get fresh auth token',
  EMPTY_AUTH_TOKEN: 'empty token data',
  EMPTY_TRANSACTION: 'empty transaction data',
  INVALID_QUEST_ADDRESS: 'invalid quest address',
  INVALID_EXPIRATION: 'invalid or missing expiration',
  INVALID_TOKEN: 'invalid or missing token',
} as const;
